# LOOana — LOOTY (looty-japan.com) 専用リポジトリ

このリポジトリは Shopify ストア **LOOTY / looty-japan.com** の損益分析・広告運用のためのものです。
別の Shopify サイトの作業は、ここでは一切行いません。

---

## 🚨 セッション開始時に必ずやること

Shopify のデータを1件でも取る前に、接続先を確認してください。

1. `mcp__Shopify__get-shop-info` を呼ぶ
2. `domain` が **looty-japan.com** であることを確認する
3. 確認した店名とドメインを、その回の最初の返答の冒頭に1行で書く
   　例: `接続先を確認しました: LOOTY / looty-japan.com ✓`

### なぜ必要か

Shopify MCP の「現在接続しているストア」は **アカウント単位で1つ** しかありません。
ユーザーは別の Shopify サイトも運営していて、そちらのセッションで接続先を切り替えます。
切り替えたまま次にこのリポジトリへ来ると、**別ストアに繋がった状態で作業を始めてしまいます。**

商品名も注文数も普通に返ってくるので、気づかないまま
「LOOTYの分析」として別店の数字を出す事故が起きます。だから毎回確認します。

### LOOTY でなかった場合

**そこで作業を止めてください。Shopify のデータを1件も取らないこと。**

ユーザーに「いま〈店名〉に繋がっています。LOOTYに戻してください」と伝える。

このリポジトリでは `mcp__Shopify__switch-shop` を `.claude/settings.json` の deny に
入れてあります。**こちらから勝手に切り替えません。** 切り替えはユーザーが行います。

別ストアのデータでレポート・分析・提案を作ることは、いかなる理由があってもしません。

---

## このリポジトリの前提

- **Shopify**: LOOTY / looty-japan.com（Advanced プラン・JPY・JST）
- **Meta広告**: Supermetrics `ds_id=FA` / `act_1124340806289175`
  （Supermetrics は `ds_accounts` でアカウントを指定する方式なので、
  　Shopify と違って接続先が勝手に変わることはない）
- **仕入れ**: CJ Dropshipping・全国送料無料
- **日次レポート**: `.claude/skills/looty-profit-report/SKILL.md` に全ルールがある。
  数字の扱い（実測のみ・推計禁止）、判定ルール、原価表はすべてそこが正。

## 書き込みについて

**ユーザーの明示的な指示がない限り、Shopify にも Meta にも書き込みません。**
Shopify / Meta の変更はすべてユーザー自身が実行します。
こちらは分析・提案・手順書・スクリプトの作成までを担当します。

## 納品物の形式

ユーザーに渡す LP 文面・広告コピー・メール文面などは **プレーンテキスト**で書きます。
HTML では書きません。
